# SDD（仕様駆動開発）開発ルール

すべての機能実装およびバグ修正において、以下のステップバイステップのSDDプロセスを厳格に遵守してください。ユーザーが設計を承認する前に、実装コードを直接書き換えてはなりません。

## フェーズ 1: 要求・仕様の確認

1. ユーザーの要望を注意深く読み込んでください。
2. 既存の関連ファイルや仕様書を確認してください。
3. 明示的な要求事項と、暗黙の前提条件をリストアップしてください。
4. まず最初に、更新または提案する「仕様のまとめ」を出力し、ユーザーの承認を待ってください。

## フェーズ 2: 設計とタスクの分解（Antigravity方式）

1. ソースコードを一切変更する前に、マークダウンファイル（例: `docs/design/機能名.md`）を作成または更新してください。
2. 以下の項目を明確に定義してください：
   - データ構造 / APIスキーマ
   - UI/UXの変更点（該当する場合）
   - ステップバイステップの実装手順（チェックボックス付きのタスクリスト）
3. このタスクリストをユーザーに提示し、「タスク1から進めてもよろしいですか？」と明示的に許可を求めてください。

## フェーズ 3: 段階的な実装

1. タスクは「1つずつ」実行してください。ユーザーから明示的な許可がない限り、複数のタスクを一度のAI回答でまとめて実装してはいけません。
2. 各タスクが完了するたびに、以下の対応を行ってください：
   - 関連するテストやビルドを実行し、正常に動作するか確認する。
   - 設計マークダウンファイル内のチェックボックスを [x] に更新する。
   - 進捗をユーザーに報告し、次のタスクに進むための許可を求める。

## 共通行動指針

- コミュニケーションはすべて日本語で行ってください。
- 曖昧さを排除し、構造化された分かりやすい表現を心がけてください。
- コードの変更と並行して、ドキュメントや設計ファイルを常に最新の状態に保つことを最優先してください。
- 【重要：コストとQuotaの最適化】
  設計書のチェックボックスが1つ以上完了したタイミング、または会話が5往復を超えたタイミングで、AI側から必ずユーザーに対して「コンテキスト（履歴）の肥大化を防ぐため、一度ここまでの内容をコミットし、新しいチャット（スレッド）に切り替えましょう」と提案をしてください。

---

# このリポジトリでの補足

## 検証コマンド

フェーズ 3 の「関連するテストやビルドを実行」はこれを使う。テストランナーは入っていない。

```
npm run lint     # eslint。既存の警告 4 件（@next/next/no-img-element）は既知
npm run build    # next build。型チェックも通る
npm run dev      # 開発サーバー
```

`no-img-element` の 4 件はどれも差し替えられない事情があり、
[`src/app/components/README.md`](src/app/components/README.md) の TODO に事情を書いてある。
これが 4 件から増えていないかを確認する。

## 設計ファイルの置き場所

`docs/design/機能名.md`。まだディレクトリが無いので、最初のタスクで作る。

## 技術的な決まりごとの在り処

コードを書く前にこちらを読む。SDD のフェーズ 2 で設計を書くときも、
ここの制約を踏まえていないと設計ごと作り直しになる。

| 場所 | 中身 |
|---|---|
| [`src/app/components/README.md`](src/app/components/README.md) | ディレクトリ構成、GSAP / WebGL の決まりごと、踏んだ落とし穴 |
| `.claude/skills/site-component/` | 上記を作業手順の形にしたスキル。コンポーネントの追加・変更時に呼ばれる |

とくに以下は破ると動かなくなるものなので、設計時点で意識する。

- 動きを減らす設定（`prefers-reduced-motion`）に必ず対応する
- `gsap.matchMedia()` は排他な 2 本を書く（片方だけだと逆の条件で丸ごと動かない）
- スクロール量で位置を決めるものは `pin` / `scrub` を使い、自前 rAF ループを書かない
- 1 つのプロパティを複数の ScrollTrigger で触らない
- 静的なずらしと GSAP のアニメーションを両方 transform で書かない
- 後片付けは `ctx.revert()` か `mm.revert()` で行う

## 画像を追加するとき

`public/` は原本の保管場所ではない。追加したら
[`scripts/optimize-images.mjs`](scripts/optimize-images.mjs) を通す。

```
npm run images         # 確認
npm run images:apply   # WebP に変換
```

## ブランチ

`main` が既定ブランチ。作業は `feature/*` を切り、そこへコミットしてから
`main` へ `--no-ff` でマージする運用にしている（`develop` も存在する）。
